import 'package:flutter/material.dart';

class PriorityTaskPage extends StatelessWidget {
  const PriorityTaskPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Priority Task'),
      ),
      body: const Center(
        child: Text('Priority Task Page'),
      ),
    );
  
  }
}
